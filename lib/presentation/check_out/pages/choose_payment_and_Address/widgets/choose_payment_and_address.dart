import 'package:flutter/material.dart';

class ChoosePaymentAndAddress extends StatelessWidget {
  const ChoosePaymentAndAddress({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

      body: ChoosePaymentAndAddress(),
    );
  }
}